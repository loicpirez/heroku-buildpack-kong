#!/usr/bin/env bash
# bin/detect <build-dir>

if [ -f $1/config/kong.conf.etlua ]; then
  echo 'Detected Kong config'
  exit 0
fi

>&2 echo 'Kong: config/kong.conf.etlua not found in application root'
exit 1
